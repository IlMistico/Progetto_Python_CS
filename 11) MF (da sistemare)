# 11. For each decade, compute the company with maximum revenue

# con questo codice viene visualizzata la massima revenue per ogni anno
overall_companies_revenue.groupby(by =['decade',])[["revenue"]].apply(max, axis = 0)


# successivamente si interroga il dataset per ogni decade

# 1910
overall_companies_revenue[overall_companies_revenue["decade"] == "1910"][overall_companies_revenue["revenue"] == 8394751]
# per vedere se è corretto
mv_norm_companies[mv_norm_companies["decade"] == "1910"]

# 1920
overall_companies_revenue[overall_companies_revenue["decade"] == "1920"][overall_companies_revenue["revenue"] == 26358000]
# per vedere se è corretto
mv_norm_companies[mv_norm_companies["decade"] == "1920"]


